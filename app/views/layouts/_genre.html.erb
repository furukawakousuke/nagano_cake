   <div class="pb-5 d-flex">

    <table class="border pb-5">
        <thead class="border-bottom">
            <tr class="px-3 py-3">
                <th class="px-4 py-2">ジャンル一覧</th>
            </tr>
        </thead>
        <% genres.each do |genre| %>
        <tbody class="pb-5">
            <tr>
                <td class="px-4 py-2 "><%= link_to genre.name,items_path(genre_id: genre.id) %></td>
            </tr>
    <% end %>
        </tbody>
    </table>
    </div>